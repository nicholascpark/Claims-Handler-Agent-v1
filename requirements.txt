# Core Dependencies for Claims Handler Agent v1 - Trustcall-Only Backend
# Based on OpenAI Chat Supervisor pattern with exclusive trustcall JSON extraction

# Essential AI and API dependencies
openai>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# REQUIRED: Trustcall and LangChain for JSON extraction (NO FALLBACKS)
trustcall>=0.2.0
langchain-openai>=0.1.0
langchain-core>=0.2.0
langgraph>=0.2.0

# Location services (preserved from original)
pgeocode
pandas

# Async support for API integration
aiohttp

# Voice Processing Dependencies for Enhanced Test Script
websockets>=12.0
pyaudio>=0.2.11
numpy>=2.0.0
sounddevice>=0.4.6

# Development and testing
pytest
pytest-asyncio
black

# Optional: FastAPI for potential web interface
fastapi
uvicorn[standard]

# Optional: Enhanced features
pytz
gunicorn

# Notes:
# - trustcall is now REQUIRED (no fallback methods available)
# - langchain-openai REQUIRED for AzureChatOpenAI integration
# - All JSON extraction uses trustcall exclusively
# - SQLAlchemy omitted (no DB state in this project)
# - asyncio is built-in to Python 3.7+, removed from requirements