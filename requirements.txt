# Core Dependencies for Claims Handler Agent v1 - Simplified Backend
# Based on OpenAI Chat Supervisor pattern

# Essential AI and API dependencies
openai>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Location services (preserved from original)
pgeocode
pandas

# Async support for API integration
aiohttp
asyncio

# Voice Processing Dependencies for Enhanced Test Script
websockets>=12.0
pyaudio>=0.2.11
numpy>=2.0.0
sounddevice>=0.4.6

# Development and testing
pytest
pytest-asyncio
black

# Optional: FastAPI for potential web interface
fastapi
uvicorn[standard]

# Optional: Enhanced features
pytz
gunicorn
sounddevice

# Note: Removed complex dependencies from previous version:
# - langchain, langgraph (replaced with direct OpenAI integration)
# - trustcall (custom payload handling)
# - SQLAlchemy (simplified state management)
# - Audio processing libs (handled by OpenAI Realtime API)
# - CORS/security (simplified for standalone operation)